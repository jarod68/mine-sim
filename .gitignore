# Test coverage output
coverage/

# Playwright visual-test artifacts (baselines under test/visual/ ARE committed)
test-results/
playwright-report/
test/visual/**/*-actual.png
test/visual/**/*-diff.png

# Robot Framework run artifacts (the .robot suites ARE committed)
test/robot/results/

# Dependencies (recommended — see note below)
node_modules/

# Runtime-generated admin password (persisted on the server's volume)
.env

# SQLite persistence (lives on the server's data volume; one DB per cluster worker)
minesim*.db
minesim*.db-wal
minesim*.db-shm
