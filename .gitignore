# Test coverage output
coverage/

# Dependencies (recommended — see note below)
node_modules/

# Runtime-generated admin password (persisted on the server's volume)
.env

# SQLite persistence (lives on the server's data volume)
minesim.db
minesim.db-wal
minesim.db-shm
